class Solution {
    public int reverseDegree(String s) {
        int sum=0;

        for(int i=0;i<s.length();i++){
            int c='z' + 1 - s.charAt(i);
            int b=c*(i+1);
            
            sum += b;
        }
        return sum;
    }
}
