class Solution {
    public String trimTrailingVowels(String s) {
        
        int i=s.length()-1;

        while(i>=0 && "AEIOUaeiou".contains(Character.toString(s.charAt(i)))){
           
                i--;
       
        }
        return s.substring(0,i+1);
    }
}
