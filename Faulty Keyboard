class Solution {
    public String finalString(String s) {
        
        StringBuilder b=new StringBuilder();

        for(char c: s.toCharArray()){
            if(c == 'i'){
                b.reverse();
            }
            if(c!='i'){
                b.append(c);
            }
        }
        return b.toString();
    }
}
